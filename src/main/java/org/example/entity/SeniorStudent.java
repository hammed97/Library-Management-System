package org.example.entity;

public class SeniorStudent extends Base{
    public SeniorStudent(String name, Integer age, Level level, String bookRequestTitle, Integer bookRequestQuantity) {
        super(name, age, level, bookRequestTitle, bookRequestQuantity);
    }
}
